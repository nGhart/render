/**
 * Converts a numeric value to a locale-specific string representation.
 * @param {number} value - The numeric value to convert.
 * @returns {string} - The locale-specific string representation of the numeric value.
 */
export default function (value) {
	return value.toLocaleString();
}
